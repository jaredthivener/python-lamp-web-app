# Development environment variables for local development
# Copy this file to .env.local and customize as needed

# Environment
ENVIRONMENT=development

# Python path
PYTHONPATH=/workspaces/python-lamp-web-app/src

# Local database configuration (for development without Azure)
# Uncomment and configure if using a local PostgreSQL instance
# POSTGRES_CONNECTION_STRING=postgresql://username:password@localhost:5432/lamp_db

# Azure development configuration (when connected to Azure resources)
# These will be set automatically when using the dev container with Azure integration
# KEY_VAULT_URI=https://your-keyvault.vault.azure.net/
# AZURE_CLIENT_ID=your-managed-identity-client-id

# Application settings
PORT=8000
DEBUG=true

# Logging level
LOG_LEVEL=INFO
